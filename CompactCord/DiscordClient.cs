using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Noemax.WebSockets;

namespace CompactCord
{
    /// <summary>
    /// I don't actually know how this all works but
    /// this will probably be the object containing a "bot"
    /// instance.
    /// </summary>
    class DiscordClient
    {

        public WebSocketClient WSInstance;

        public DiscordClient()
        {
            // stub
        }
    }
}
